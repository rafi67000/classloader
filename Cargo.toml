[package]
name = "classloader"
version = "0.1.0"
edition = "2024"

[lib]
name = "classloader"
crate-type = ["cdylib"]

[dependencies]
robusta_jni = "0.2"
jni = "0.22.4"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"